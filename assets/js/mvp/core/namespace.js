(function (global) {
	var app = global.EverloftMVP || {};

	app.Models = app.Models || {};
	app.Views = app.Views || {};
	app.Presenters = app.Presenters || {};

	global.EverloftMVP = app;
})(window);
